# Load environment variables for UI Chatter service
# .env.local overrides .env for local development

# Load base configuration
dotenv_if_exists .env

# Load local overrides (gitignored)
dotenv_if_exists .env.local
