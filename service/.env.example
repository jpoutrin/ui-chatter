# UI Chatter Service Configuration

# Service Configuration
PROJECT_PATH=.
DEBUG=false
HOST=localhost
PORT=3456
LOG_LEVEL=INFO

# Claude Agent SDK Backend (subscription-based auth from ~/.claude/config)
# Uses Claude Max subscription, no API key needed
# Authenticate: claude login

# Storage
MAX_SCREENSHOT_AGE_HOURS=24
MAX_SESSION_IDLE_MINUTES=30

# Security
MAX_CONNECTIONS=100

# Performance
WORKER_COUNT=1
